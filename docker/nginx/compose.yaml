services:
  nginx:
    image: nginx:latest
    container_name: notification_nginx
    env_file: ../../.env
    volumes:
      - ../../project:/var/www
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - ${APP_PORT}:80
    depends_on:
      - app